import { FormatTextHtmlPipe } from './format-text-html-pipe';

describe('FormatTextHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTextHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
